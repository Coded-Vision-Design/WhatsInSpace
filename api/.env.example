# Database connection (Hostinger MySQL)
DB_HOST=your-db-host.hstgr.io
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASS=your_database_password

# API key for protected POST endpoints (generate a strong random string)
API_KEY=your-secret-api-key-here

# Allowed origin for CORS (your frontend domain)
ALLOWED_ORIGIN=https://whatsthatin.space
